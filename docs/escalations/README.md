# Escalations — asfdk-csharp

Escalation records to the Human Principal (per `templates/escalation.md`) are stored in this directory.

**2026-09-12 (THREAD-001, PR #1):** None raised. No architectural decisions, external service integrations, LLM provider lock-ins, or credentials were involved; the PR's change restores behavior already implied by the existing `FoundationComponents` type and the TypeScript reference.